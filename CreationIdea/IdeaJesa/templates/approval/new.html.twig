{% extends 'base.html.twig' %}

{% block title %}New Approval{% endblock %}

{% block body %}
    <h1>Create new Approval</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('approval_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
